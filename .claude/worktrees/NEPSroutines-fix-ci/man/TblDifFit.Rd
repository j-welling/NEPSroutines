% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/technical_report_tables.r
\name{TblDifFit}
\alias{TblDifFit}
\alias{TblDIFFit}
\title{Create table with fit statistics for DIF analyses}
\usage{
TblDifFit(
  obj,
  footnote = NULL,
  excl = NULL,
  label = NULL,
  size = 12,
  width = 0.9
)

TblDIFFit(
  obj,
  footnote = NULL,
  excl = NULL,
  label = NULL,
  size = 12,
  width = 0.9
)
}
\arguments{
\item{obj}{A list with data frames with sheets from "dif_poly_TR.xlsx"
or a data frame with sheet "estimates" from "dif_poly_TR.xlsx" created by
[dif_analysis()].}

\item{footnote}{A table note.}

\item{excl}{A vector of DIF variables that should be excluded.}

\item{label}{A vector of names for the DIF variables.}

\item{size}{The general font size.}

\item{width}{The widths of the columns; if a single values is given, it
corresponds to the first column; otherwise the number of values must
correspond to the number of columns in `obj`.}
}
\value{
A flextable.
}
\description{
Create table with fit statistics for DIF analyses
}
\examples{
\dontrun{
data(ex1)

# DIF analyses
dif_analysis(
 resp = ex1$resp,
 vars = ex1$vars,
 select = "dich",
 valid = "valid",
 dif_vars = c("sex", "mig"),
 include_mv = 100,
 print = FALSE,
 save = TRUE,
 return = FALSE,
 path_results = tmpdir,
 path_table = tmpdir,
 overwrite = TRUE,
 verbose = FALSE,
 warn = FALSE
)

# Import results of DIF analyses
dif <- Import(tmpdir, regexp = "^dif_dich_([^_]+\\\\.xlsx)")

# Default DIF fit table
TblDifFit(dif$TR)

# Excluding results for sex, but with a new label for mig
TblDifFit(dif$TR, excl= "sex", label = c("mig" = "Migrant background"))
}
}
